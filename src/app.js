import Vue from 'vue';
const axios = require('axios').default;

var root = new Vue(
  {
    el: '#root',
    data: {
      discs: [],
    },
    methods: {
    },
    mounted: function() {
      
    }
  }
)
